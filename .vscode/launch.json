{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}",
            "preLaunchTask": "generate",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}